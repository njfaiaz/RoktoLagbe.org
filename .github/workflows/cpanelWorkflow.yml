# Live project uploaded to cPanel without  Assets file all updated.
name: Deploy to cPanel on Production Branch
on:
  push:
    branches:
      - production
jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      #Deploy to cPanel
      - name: FTP-Deploy-Action
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: 21
# # Live project uploaded to cPanel with Assets file all updated.

# name: Deploy Only Assets to cPanel

# on:
#   push:
#     branches:
#       - production

# jobs:
#   FTP-Deploy-Assets:
#     name: 🎯 Deploy Updated Assets to /public_html/assets/
#     runs-on: ubuntu-latest

#     steps:
#       - name: ⬇️ Checkout Code
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 2

#       - name: 📁 Prepare Asset Files
#         run: |
#           mkdir -p deploy_assets
#           cp -r public/assets/* deploy_assets/

#       - name: 🚀 FTP Upload assets to assets/
#         uses: SamKirkland/FTP-Deploy-Action@v4.3.4
#         with:
#           server: ${{ secrets.FTP_SERVER }}
#           username: ${{ secrets.FTP_USERNAME }}
#           password: ${{ secrets.FTP_PASSWORD }}
#           port: 21
#           server-dir: /assets/
#           local-dir: deploy_assets/
#           protocol: ftp
#           dangerous-clean-slate: false # Only overwrite changed files

#       - name: ✅ Finished
#         run: echo "Assets uploaded to production server."
